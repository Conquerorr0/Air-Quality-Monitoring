<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragment_home2">
    <fragment
        android:id="@+id/fragment_home2"
        android:name="com.fatihaltuntas.airqualityindex.view.fragment_home"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_fragment_home2_to_fragmentHistory"
            app:destination="@id/fragmentHistory" />
    </fragment>
    <fragment
        android:id="@+id/fragmentHistory"
        android:name="com.fatihaltuntas.airqualityindex.view.FragmentHistory"
        android:label="fragment_history"
        tools:layout="@layout/fragment_history" >
        <action
            android:id="@+id/action_fragmentHistory_to_fragment_home2"
            app:destination="@id/fragment_home2" />
    </fragment>
</navigation>